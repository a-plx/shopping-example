# To create these indexes, run:
#    $ gcloud preview datastore create-indexes index.yaml
#
# It will take some time for the indices to be created.

indexes:

# This index enables filtering by "Description" and sort by "Title".
- kind: Offer
  properties:
  - name: Description
    direction: asc
  - name: Title
    direction: asc
